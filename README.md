# Sauvegardes automatiques

Cette branche contient uniquement les sauvegardes hebdomadaires de la base de données (dossier `backups/<date>/`), générées automatiquement par une GitHub Action. Ne pas y développer de code — utilisez `main`/`dev` pour ça.
